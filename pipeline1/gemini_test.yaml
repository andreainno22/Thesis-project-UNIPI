# Gemini-generated obstruction test set (single-class), for detection mAP.
# All images are positives (obstruction present), both venues hand-annotated:
#   Dataset/ostruzioni_gemini/corridoi/  (34 images)
#   Dataset/ostruzioni_gemini/porte/     (60 images)
# Ultralytics resolves images/ -> labels/ automatically, so each venue keeps
# its own images/<venue>/ + labels/<venue>/ pair.
# Usage:
#   yolo val model=path/to/best.pt data=pipeline1/gemini_test.yaml
path: C:/Users/andrea/OneDrive - University of Pisa/Università/Unipi/tesi/Dataset/ostruzioni_gemini
train: corridoi/images/corridoi  # unused placeholder, this yaml is val-only
val:
  - corridoi/images/corridoi
  - porte/images/porte
nc: 1
names: [obstruction]
